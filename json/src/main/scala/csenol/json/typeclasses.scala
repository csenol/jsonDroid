package csenol.json.readers

/*
trait JsonReaderHelper[T] extends JsonReaderT[T]{

  def read(in: JsonReader): T = readHelper(readMatcher)
  def readMatcher: PartialFunction[String, T]
  def readHelper(in: JsonReader) = {
    while(in.hasNext){
    readMatcher(in.nextName)
    }
  }

}
*/

/*
trait JsonWriterWrapper[T] {

  def writeJson(out: T, jw: JsonWriter):Unit

}

*/

